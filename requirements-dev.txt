# Development / CI dependencies. Not installed into the Docker image.
#     pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0          # handle_turn and the whole orchestrator are async
