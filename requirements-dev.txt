# Development / CI dependencies. Not installed into the Docker image.
#     pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
# No pytest-asyncio: every test under tests/ is synchronous.
